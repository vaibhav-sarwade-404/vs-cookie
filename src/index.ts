import VsCookie from "./VsCookie";

const createCookie = VsCookie.createCookie;
const getCookie = VsCookie.getCookie;
const parse = VsCookie.parse;
const sign = VsCookie.sign;
const verify = VsCookie.verify;

export { createCookie, getCookie, parse, sign, verify };
